<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <link rel="stylesheet" href="{{ asset('bundles/saiduldomainmanagement/css/style.css') }}" type="text/css" media="all" />
        <title>{% block title %}Site Title{% endblock %}</title>
        <link rel="shortcut icon" href="{{ asset('favicon.ico') }}" />
        <script type="text/javascript" src="{{ asset('bundles/saiduldomainmanagement/js/jquery-1.5.1.min.js') }}"></script>
        <script type="text/javascript" src="{{ asset('bundles/saiduldomainmanagement/js/main.js') }}"></script>
    </head>
    <body>
    <div class="header">
        <h1>DManager</h1>
        <span>A simple domain management tool.</span>
    </div>
    <div class="wrapper">
    {%if sMsg is defined and sMsg|length > 0 %}
        <div class="successMsg">{{sMsg}}</div>
    {%endif%}
    {%if eMsg is defined and  eMsg|length > 0 %}
        <div class="errorMsg">{{sMsg}}</div>
    {%endif%}
        {%block body%}
        {%endblock%}
    </div>
    </body>
</html>
